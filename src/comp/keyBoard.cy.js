import { expect } from "chai"
import { KeyBoard } from "./KeyBoard"
describe('', ()=>{
    it('keyBoard testing', () => {
        // cy.mount(<KeyBoard/>)
        // cy.get('button').first().should('have.text','Q')
        expect(true).to.be.equal(true)
      })
})
